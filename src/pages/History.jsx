import React,{useEffect,useState}from'react';import{supabase}from'../lib/supabase.js';import{requireAuth}from'../lib/auth.js';import*as S from'../styles.js';
export default function History(){const[s,setS]=useState([]);useEffect(()=>{requireAuth(window.location.href);supabase.from('lens_scans').select('*').order('created_at',{ascending:false}).then(({data})=>setS(data||[]));  },[]);
return React.createElement('div',{style:S.page},React.createElement('h1',{style:S.h1},'Scan History'),s.length===0&&React.createElement('div',{style:S.card},React.createElement('p',{style:S.muted},'No scans yet.')),s.map(x=>React.createElement('div',{key:x.id,style:{...S.card,cursor:'pointer'},onClick:()=>window.location.href='/result/'+x.id},x.image_url&&React.createElement('img',{src:x.image_url,style:{maxWidth:'120px',height:'80px',objectFit:'cover',borderRadius:'6px',marginBottom:'0.5rem',display:'block'}}),React.createElement('p',{style:S.muted},new Date(x.created_at).toLocaleString()))));}